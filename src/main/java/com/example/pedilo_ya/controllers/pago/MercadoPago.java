package com.example.pedilo_ya.controllers.pago;

import com.mercadopago.exceptions.MPException;

public class MercadoPago {
    /*
    public static void main(String[] args) {
        // Configura las credenciales de MercadoPago
        MercadoPago.SDK.setAccessToken("TOKEN PRIVADO");

        try {
            // Crea un objeto de preferencia
            Preference preference = new Preference();

            // Crea un ítem en la preferencia
            Item item = new Item();
            item.setTitle("Mi producto");
            item.setQuantity(1);
            item.setUnitPrice(new BigDecimal("75"));

            List<Item> items = new ArrayList<>();
            items.add(item);

            preference.setItems(items);
            preference.setPurpose("wallet_purchase");

            // Crea la preferencia en MercadoPago
            preference.save();

            // Obtiene el ID de la preferencia generada
            String preferenceId = preference.getId();

            System.out.println("ID de Preferencia: " + preferenceId);
        } catch (MPException e) {
            System.err.println("Error al crear la preferencia: " + e.getMessage());
        }


    }
    */
}

